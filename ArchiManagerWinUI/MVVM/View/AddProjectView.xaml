<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.AddProjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Border VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_InfoAdd}">
                <StackPanel>
                    <TextBlock Text="PROYECTO" Style="{StaticResource TBLOCK_TITLE}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding ExpedientNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nº Expediente" PlaceholderText="22_67" Width="150" HorizontalAlignment="Left"/>
                                <Button Command="{Binding SearchFolderCommand}" Background="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,0">
                                    <Image Source="/Assets/Icons/botonBuscarCarpeta.png" Height="40"/>
                                </Button>
                            </StackPanel>
                            <TextBox Text="{Binding ArchiveNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nº Archivo" PlaceholderText="212" Width="150" HorizontalAlignment="Left"/>
                            <TextBox Text="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Año" PlaceholderText="2025" Width="150" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding CadastralReference, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Referencia Catastral" PlaceholderText="1234567AA12345" Width="280" HorizontalAlignment="Left"/>
                                <Button Command="{Binding SearchCatRefCommand}" Background="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,0">
                                    <Image Source="/Assets/Icons/botonBuscarWeb.png" Height="40"/>
                                </Button>
                            </StackPanel>
                            <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Título" PlaceholderText="Obra Marcelino" Width="280" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBox Text="{Binding Comment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Comentario" PlaceholderText="El proyecto se encuentra en buen estado" Height="200" Width="300" TextWrapping="Wrap"/>
                        </StackPanel>

                    </Grid>
                </StackPanel>
            </Border>

            <!-- Imágen opcional -->
            <Border Margin="20,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_InfoAdd}">
                <StackPanel>
                    <TextBlock Text="IMÁGEN OPCIONAL" Style="{StaticResource TBLOCK_TITLE}"/>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel HorizontalAlignment="Center">
                            <Image  Source="{Binding ImageBind}" Width="200" Height="200" Margin="20"/>
                            <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="{Binding ImageName, Mode=OneWay}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Margin="0,20,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Button Command="{Binding AddImageCommand}" Background="LightGray" HorizontalAlignment="Center">
                                <Image Source="/Assets/Icons/botonAgregar.png" Height="50"/>
                            </Button>
                            <Button Margin="0,20,0,0" Command="{Binding RemoveImageCommand}" Background="LightGray" HorizontalAlignment="Center">
                                <Image Source="/Assets/Icons/botonEliminar.png" Height="50"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
        
        
        <!-- Parte de abajo -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">

            <!-- Cliente a asignar -->
            <Border VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_InfoAdd}">
                <StackPanel>
                    <TextBlock Text="CLIENTE ASOCIADO" Style="{StaticResource TBLOCK_TITLE}"/>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel>
                            <TextBox Text="{Binding Dni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="DNI:" PlaceholderText="12345678Z" Width="200" HorizontalAlignment="Left"/>
                            <TextBox Text="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Teléfono" PlaceholderText="+34986738291" Width="200" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nombre:" PlaceholderText="Juan" Width="250" HorizontalAlignment="Left"/>
                            <TextBox Text="{Binding Surname1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 1:" PlaceholderText="García" Width="250" HorizontalAlignment="Left"/>
                            <TextBox Text="{Binding Surname2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 2:" PlaceholderText="González" Width="250" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBox Text="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Dirección" PlaceholderText="Avenida Desengaño 21" Width="300" Height="150" TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            
            <Border Grid.Row="0" BorderBrush="Black" BorderThickness="0,0,1,0" Margin="0,20,0,20"/>
            
            <!-- SP opciones -->
            <Border Width="300" Margin="20,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                <StackPanel Orientation="Vertical">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="AÑADIR" Style="{StaticResource TBLOCK_Normal}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="40,0,0,0"/>
                        <Button Grid.Column="1" Command="{Binding AddProjectCommand}" Background="LightGray" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,10,20,10">
                            <Image Source="/Assets/Icons/botonAgregar.png" Height="85"/>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
