<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.ClientListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="20,10,20,0">
            <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="Filtros de búsqueda" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ComboBox Style="{StaticResource CB_Normal}" PlaceholderText="Columna a filtrar" SelectedItem="{Binding SelectedColumn, Mode=TwoWay}">
                    <x:String>Nombre</x:String>
                    <x:String>Apellido 1</x:String>
                    <x:String>Apellido 2</x:String>
                    <x:String>DNI</x:String>
                </ComboBox>
                <ComboBox Margin="20,0,0,0" Style="{StaticResource CB_Normal}" PlaceholderText="Orden de filtrado" SelectedItem="{Binding SelectedOrder, Mode=TwoWay}">
                    <x:String>Ascendente</x:String>
                    <x:String>Descendente</x:String>
                </ComboBox>
            </StackPanel>
            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nombre:" PlaceholderText="Juan" Width="300" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Surname1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 1:" PlaceholderText="García" Width="300" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Surname2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 2:" PlaceholderText="González" Width="300" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Dni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="DNI:" PlaceholderText="12345678Z" Width="250" HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,0">
                <Border Width="125" Height="150" Margin="0,20,20,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                    <StackPanel HorizontalAlignment="Center">
                        <Button Command="{Binding SearchClientCommand}" CommandParameter="0" Background="LightGray">
                            <Image Source="/Assets/Icons/botonBuscar.png" Height="90"/>
                        </Button>
                        <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="BUSCAR" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Width="125" Height="150" Margin="0,20,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                    <StackPanel HorizontalAlignment="Center">
                        <Button Command="{Binding ExportClientsCommand}" CommandParameter="0" Background="LightGray">
                            <Image Source="/Assets/Icons/botonExportar.png" Height="90"/>
                        </Button>
                        <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="EXPORTAR" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                <Button Command="{Binding SearchPrevPageCommand}" IsEnabled="{Binding CanGoBack}" Background="LightGray">
                    <Image Source="/Assets/Icons/flechaIzquierda.png" Height="80"/>
                </Button>
                <Button Margin="30,0,0,0" Command="{Binding SearchNextPageCommand}" IsEnabled="{Binding CanGoForw}" Background="LightGray">
                    <Image Source="/Assets/Icons/flechaDerecha.png" Height="80"/>
                </Button>
            </StackPanel>
        </StackPanel>

        <!-- DataGrid -->
        <controls:DataGrid Grid.Column="1" x:Name="ClientDG" Style="{StaticResource DG_Normal}" ItemsSource="{Binding DataGridClientsCollection}" Margin="20" HorizontalAlignment="Left" VerticalAlignment="Top">
            <controls:DataGrid.Columns>
                <!-- Columna con botón -->
                <controls:DataGridTemplateColumn Header="INFO">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="ClientInfo_Click" Background="Transparent" HorizontalAlignment="Center">
                                <Image Source="/Assets/Icons/botonBuscar.png" Width="24" Height="24"/>
                            </Button>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <!-- Columna con botón -->
                <controls:DataGridTemplateColumn Header="EDITAR">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="ClientEdit_Click" Background="Transparent" HorizontalAlignment="Center">
                                <Image Source="/Assets/Icons/botonModificar.png" Width="24" Height="24"/>
                            </Button>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <controls:DataGridTextColumn Header="Nombre" Binding="{Binding Name}" />
                <controls:DataGridTextColumn Header="Apellido 1" Binding="{Binding Surname1}" />
                <controls:DataGridTextColumn Header="Apellido 2" Binding="{Binding Surname2}" />
                <controls:DataGridTextColumn Header="DNI" Binding="{Binding Dni}" />
                <controls:DataGridTextColumn Header="Teléfono" Binding="{Binding Phone}" />
                <controls:DataGridTextColumn Header="Dirección" Binding="{Binding Address}" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
