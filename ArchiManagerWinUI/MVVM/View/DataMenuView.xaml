<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.DataMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="Gestionar clientes" Style="{StaticResource B_Menu}" Command="{Binding GoToManageClientsCommand}"/>
                <Button Grid.Row="1" Content="Listado clientes" Style="{StaticResource B_Menu}" Command="{Binding GoToClientListCommand}"/>
            </Grid>

            <Grid Grid.Column="1">
                <Image Source="/Assets/Logo/LogoArchiManager.png" Height="200"/>
            </Grid>
            
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="Gestionar proyecto o asociar clientes existentes" Style="{StaticResource B_Menu}" Command="{Binding GoToManageProjectsCommand}"/>
                <Button Grid.Row="1" Content="Listado proyectos" Style="{StaticResource B_Menu}" Command="{Binding GoToProjectListCommand}"/>
            </Grid>
            

        </Grid>
    </Grid>
</Page>
