<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.ProjectListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="20,10,20,0">
            <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="Filtros de búsqueda" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ComboBox Style="{StaticResource CB_Normal}" PlaceholderText="Columna a filtrar" SelectedItem="{Binding SelectedColumn, Mode=TwoWay}">
                    <x:String>Título</x:String>
                    <x:String>NºExp.</x:String>
                    <x:String>Año</x:String>
                    <x:String>Ref.Cat.</x:String>
                </ComboBox>
                <ComboBox Margin="20,0,0,0" Style="{StaticResource CB_Normal}" PlaceholderText="Orden de filtrado" SelectedItem="{Binding SelectedOrder, Mode=TwoWay}">
                    <x:String>Ascendente</x:String>
                    <x:String>Descendente</x:String>
                </ComboBox>
            </StackPanel>
            <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource TBOX_Normal}" Header="Título:" PlaceholderText="Obra Marcelino" Width="300" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding CadastralReference, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Referencia Catastral:" PlaceholderText="12345678910ABC" Width="300" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding ExpedientNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nº Expediente:" PlaceholderText="22_67" Width="150" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Año:" PlaceholderText="2025" Width="150" HorizontalAlignment="Left"/>
            
            <Border Width="125" Height="150" Margin="0,20,20,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                <StackPanel HorizontalAlignment="Center">
                    <Button Command="{Binding SearchProjectCommand}" Background="LightGray">
                        <Image Source="/Assets/Icons/botonBuscar.png" Width="100" Height="90"/>
                    </Button>
                    <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="BUSCAR" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
                <Button Command="{Binding SearchPrevPageCommand}" IsEnabled="{Binding CanGoBack}" Background="LightGray">
                    <Image Source="/Assets/Icons/flechaIzquierda.png" Height="80"/>
                </Button>
                <Button Margin="40,0,0,0" Command="{Binding SearchNextPageCommand}" IsEnabled="{Binding CanGoForw}" Background="LightGray">
                    <Image Source="/Assets/Icons/flechaDerecha.png" Height="80"/>
                </Button>
            </StackPanel>
        </StackPanel>

        <controls:DataGrid Grid.Column="1" Style="{StaticResource DG_Normal}" ItemsSource="{Binding DataGridProjectsCollection}" Margin="20"  HorizontalAlignment="Left" VerticalAlignment="Top">
            <controls:DataGrid.Columns>
                <!-- Columna con botón -->
                <controls:DataGridTemplateColumn Header="INFO">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="ProjectInfo_Click" Background="Transparent" HorizontalAlignment="Center">
                                <Image Source="/Assets/Icons/botonBuscar.png" Width="24" Height="24"/>
                            </Button>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <!-- Columna con botón -->
                <controls:DataGridTemplateColumn Header="EDITAR">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="ProjectEdit_Click" Background="Transparent" HorizontalAlignment="Center">
                                <Image Source="/Assets/Icons/botonModificar.png" Width="24" Height="24"/>
                            </Button>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <controls:DataGridTextColumn Header="Título" Binding="{Binding Title}"/>
                <controls:DataGridTextColumn Header="Nº Expediente" Binding="{Binding ExpedientNumber}"/>
                <controls:DataGridTextColumn Header="Año" Binding="{Binding Year}"/>
                <controls:DataGridTextColumn Header="Referencia Catastral" Binding="{Binding CadastralReference}"/>
                <controls:DataGridTextColumn Header="Nº Archivo" Binding="{Binding ArchiveNumber}"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>
