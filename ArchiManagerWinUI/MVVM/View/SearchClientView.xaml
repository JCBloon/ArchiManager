<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.SearchClientView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="20">
            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nombre:" PlaceholderText="Juan"/>
            <TextBox Text="{Binding Surname1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 1:" PlaceholderText="García"/>
            <TextBox Text="{Binding Surname2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 2:" PlaceholderText="González"/>
            <TextBox Text="{Binding Dni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="DNI:" PlaceholderText="12345678Z"/>
            <Border Width="125" Height="150" Margin="0,40,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                <StackPanel HorizontalAlignment="Center">
                    <Button Command="{Binding FindClientCommand}" Background="LightGray">
                        <Image Source="/Assets/Icons/botonBuscar.png" Height="100"/>
                    </Button>
                    <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="BUSCAR" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource BSP_Info}" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TBLOCK_TITLE}" Text="Datos cliente"/>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel>
                                <TextBox Text="{Binding FoundClient.Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Nombre" IsReadOnly="True" Width="250" HorizontalAlignment="Left"/>
                                <TextBox Text="{Binding FoundClient.Surname1, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Apellido 1" IsReadOnly="True" Width="250" HorizontalAlignment="Left"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBox Text="{Binding FoundClient.Dni, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="DNI" IsReadOnly="True" Width="200" HorizontalAlignment="Left"/>
                                <TextBox Text="{Binding FoundClient.Surname2, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Apellido 2" IsReadOnly="True" Width="250" HorizontalAlignment="Left"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBox Text="{Binding FoundClient.Phone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Teléfono" IsReadOnly="True" Width="200" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBox Text="{Binding FoundClient.Address, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Dirección" IsReadOnly="True" Width="500" Height="150" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Margin="20" Style="{StaticResource BSP_Info}" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TBLOCK_TITLE}" Text="Proyectos asociados" HorizontalAlignment="Center"/>
                        <controls:DataGrid Style="{StaticResource DG_Normal}" ItemsSource="{Binding DataGridProjectsCollection}" Margin="20">
                            <controls:DataGrid.Columns>
                                <controls:DataGridTemplateColumn Header="INFO">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="ProjectInfo_Click" Background="Transparent" HorizontalAlignment="Center">
                                                <Image Source="/Assets/Icons/botonBuscar.png" Width="24" Height="24"/>
                                            </Button>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>
                                <controls:DataGridTextColumn Header="Título" Binding="{Binding Title}" />
                                <controls:DataGridTextColumn Header="Nº Expediente" Binding="{Binding ExpedientNumber}" />
                                <controls:DataGridTextColumn Header="Año" Binding="{Binding Year}" />
                                <controls:DataGridTextColumn Header="Referencia Catastral" Binding="{Binding CadastralReference}" />
                                <controls:DataGridTextColumn Header="Nº Archivo" Binding="{Binding ArchiveNumber}" />
                            </controls:DataGrid.Columns>
                        </controls:DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
            
        </StackPanel>

        
        
    </Grid>
    
</Page>
