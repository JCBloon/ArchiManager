<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.SearchProjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3.5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="20">
            <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource TBOX_Normal}" Header="Título:" PlaceholderText="Obra Marcelino" Width="300" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding CadastralReference, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Referencia Catastral:" PlaceholderText="1234567AA12345" Width="300" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding ExpedientNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nº Expediente:" PlaceholderText="22_67" Width="150" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <TextBox Text="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Año:" PlaceholderText="2025" Width="150" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <Border Width="125" Height="150" Margin="0,40,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                <StackPanel HorizontalAlignment="Center">
                    <Button Command="{Binding FindProjectCommand}" Background="LightGray">
                        <Image Source="/Assets/Icons/botonBuscar.png" Width="100" Height="100"/>
                    </Button>
                    <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="BUSCAR" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="0,20,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource BSP_Info}" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource TBLOCK_TITLE}" Text="Datos proyecto"/>
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel>
                                        <TextBox Text="{Binding ProjectFound.Title, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Título" IsReadOnly="True" Width="300" HorizontalAlignment="Left"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Text="{Binding ProjectFound.CadastralReference, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Referencia Catastral" IsReadOnly="True" Width="300" HorizontalAlignment="Left"/>
                                            <Button Command="{Binding SearchCatRefCommand}" Background="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,0">
                                                <Image Source="/Assets/Icons/botonBuscarWeb.png" Height="40"/>
                                            </Button>
                                        </StackPanel>
                                        <TextBox Text="{Binding ProjectFound.Comment, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Comentario" IsReadOnly="True" Width="300" Height="150" TextWrapping="Wrap" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Text="{Binding ProjectFound.ExpedientNumber, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Nº Expediente" IsReadOnly="True" Width="150" HorizontalAlignment="Left"/>
                                            <Button Command="{Binding SearchFolderCommand}" Background="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,0">
                                                <Image Source="/Assets/Icons/botonBuscarCarpeta.png" Height="40"/>
                                            </Button>
                                        </StackPanel>
                                        <TextBox Text="{Binding ProjectFound.ArchiveNumber, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Nº Archivo" IsReadOnly="True" Width="150" HorizontalAlignment="Left"/>
                                        <TextBox Text="{Binding ProjectFound.Year, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal_OW}" Header="Año" IsReadOnly="True" Width="150" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                            <Image Grid.Column="1" Source="{Binding ImageBind}" Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0,10,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
                

                <Border Grid.Row="1" Margin="20" Style="{StaticResource BSP_Info}" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Style="{StaticResource TBLOCK_TITLE}" Text="Clientes asociados" HorizontalAlignment="Center"/>
                        <controls:DataGrid x:Name="ProjectDG" Style="{StaticResource DG_Normal}" ItemsSource="{Binding DataGridClientsCollection}" Margin="20">
                            <controls:DataGrid.Columns>
                                <!-- Columna con botón -->
                                <controls:DataGridTemplateColumn Header="INFO">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="ClientInfo_Click" Background="Transparent" HorizontalAlignment="Center">
                                                <Image Source="/Assets/Icons/botonBuscar.png" Width="24" Height="24"/>
                                            </Button>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>
                                <controls:DataGridTextColumn Header="Nombre" Binding="{Binding Name}" />
                                <controls:DataGridTextColumn Header="Apellido 1" Binding="{Binding Surname1}" />
                                <controls:DataGridTextColumn Header="Apellido 2" Binding="{Binding Surname2}" />
                                <controls:DataGridTextColumn Header="DNI" Binding="{Binding Dni}" />
                                <controls:DataGridTextColumn Header="Teléfono" Binding="{Binding Phone}" />
                                <controls:DataGridTextColumn Header="Dirección" Binding="{Binding Address}" />
                            </controls:DataGrid.Columns>
                        </controls:DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
            
        </StackPanel>

        


    </Grid>
</Page>
