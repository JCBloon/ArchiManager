<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArchiManagerWinUI.MVVM.View.ManageClientsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchiManagerWinUI.MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Style="{StaticResource GRID_Main}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <!-- Parte superior -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center" Margin="10,0,10,0">
                <StackPanel>
                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nombre:" PlaceholderText="Juan" Width="300" HorizontalAlignment="Left"/>
                    <TextBox Text="{Binding Surname1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 1:" PlaceholderText="García" Width="300" HorizontalAlignment="Left"/>
                </StackPanel>
            </StackPanel>

            <StackPanel VerticalAlignment="Center" Margin="10,0,10,0">
                <TextBox Text="{Binding Dni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="DNI:" PlaceholderText="12345678Z" Width="250" HorizontalAlignment="Left"/>
                <TextBox Text="{Binding Surname2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 2:" PlaceholderText="González" Width="300" HorizontalAlignment="Left"/>
            </StackPanel>

            <Border Width="125" Height="150" Margin="40,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                <StackPanel HorizontalAlignment="Center">
                    <Button Command="{Binding FindClientCommand}" Background="LightGray">
                        <Image Source="/Assets/Icons/botonBuscar.png" Height="100"/>
                    </Button>
                    <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="BUSCAR" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.75*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            
        </Grid>

        <Border BorderBrush="Black" BorderThickness="0,0,0,1" Margin="20,0,20,0"/>

        <!-- Parte inferior -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Parte izquierda -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="5,20,5,20" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_InfoModify}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBox Text="{Binding FoundClient.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Nombre" Width="200"/>
                                <TextBox Text="{Binding FoundClient.Surname1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 1" Width="200"/>
                                <TextBox Text="{Binding FoundClient.Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Dirección" Width="200" TextWrapping="Wrap" Height="150"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBox Text="{Binding FoundClient.Dni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="DNI" Width="200" HorizontalAlignment="Left"/>
                                <TextBox Text="{Binding FoundClient.Surname2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Apellido 2" Width="200" HorizontalAlignment="Left"/>
                                <TextBox Text="{Binding FoundClient.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TBOX_Normal}" Header="Teléfono" Width="200"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Width="350" Height="140" Margin="0,0,0,10" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource BSP_Normal}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <StackPanel>
                                    <Button Command="{Binding UpdateClientCommand}" Background="LightGray" HorizontalAlignment="Center">
                                        <Image Source="/Assets/Icons/botonModificar.png" Height="80"/>
                                    </Button>
                                    <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="MODIFICAR" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Column="1">
                                <StackPanel>
                                    <Button Command="{Binding DeleteClientCommand}" Background="LightGray" HorizontalAlignment="Center">
                                        <Image Source="/Assets/Icons/botonEliminar.png" Height="80"/>
                                    </Button>
                                    <TextBlock Style="{StaticResource TBLOCK_Normal}" Text="ELIMINAR" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1,0,0,0" Margin="10,20,0,20"/>

            <!-- Parte derecha -->
                <!-- Tabla proyectos -->
                <Border Grid.Column="1" Margin="30" Style="{StaticResource BSP_Info}" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <StackPanel>
                        <controls:DataGrid x:Name="ProjectDG" Style="{StaticResource DG_Normal}" ItemsSource="{Binding DataGridProjectsCollection}" Margin="20" HorizontalAlignment="Center">
                            <controls:DataGrid.Columns>
                                <controls:DataGridTextColumn Header="Título" Binding="{Binding Title}"/>
                                <controls:DataGridTextColumn Header="Nº Expediente" Binding="{Binding ExpedientNumber}"/>
                                <controls:DataGridTextColumn Header="Año" Binding="{Binding Year}"/>
                                <controls:DataGridTextColumn Header="Referencia Catastral" Binding="{Binding CadastralReference}"/>
                                <controls:DataGridTextColumn Header="Nº Archivo" Binding="{Binding ArchiveNumber}"/>

                                <!-- Columna con botón -->
                                <controls:DataGridTemplateColumn Header="Desasignar">
                                    <controls:DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="UnassignButton_Click" Background="Transparent" HorizontalAlignment="Center">
                                                <Image Source="/Assets/Icons/botonEliminar.png" Width="24" Height="24"/>
                                            </Button>
                                        </DataTemplate>
                                    </controls:DataGridTemplateColumn.CellTemplate>
                                </controls:DataGridTemplateColumn>
                            </controls:DataGrid.Columns>
                        </controls:DataGrid>
                    </StackPanel>
                </Border>
        </Grid>

    </Grid>
</Page>
