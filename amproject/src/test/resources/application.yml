spring:
  datasource:
    url: jdbc:mysql://localhost:10200/archidbtest
    username: archiuser
    password: archi
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: true
      connection-timeout: 10000
      transaction-isolation: TRANSACTION_SERIALIZABLE

  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
